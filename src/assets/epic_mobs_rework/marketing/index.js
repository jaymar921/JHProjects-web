/**
 * The store art for both Epic Mobs Rework listings, copied out of the plugin
 * repository's marketing folder rather than redrawn here.
 *
 * These are the images the Spigot posts carry. Keeping the page on the same
 * set means somebody who arrives from the listing recognises where they are,
 * and there is only one place to change them: the generator in the plugin
 * repo at marketing/tools/generate-attachments.mjs.
 */

import hero from "./hero.png";
import mobs from "./mobs.png";
import world from "./world.png";
import integration from "./integration.png";
import liteHero from "./lite-hero.png";
import liteScope from "./lite-scope.png";

export { hero, mobs, world, integration, liteHero, liteScope };
