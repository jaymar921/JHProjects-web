/**
 * The 1.6.0 store page set, copied straight out of
 * marketing/advertising_1.6.0_attachments in the plugin repo.
 *
 * There is no "systems" image in this set. That one is still the 1.5.0 draw
 * and it is still accurate, so the page keeps importing it from
 * ../marketing_1_5_0 rather than a re-cut that would say the same thing.
 */

import hero from "./ce3-1.6.0-hero.png";
import economy from "./ce3-1.6.0-economy.png";
import enchants from "./ce3-1.6.0-enchants.png";
import update from "./ce3-1.6.0-update.png";

export { hero, economy, enchants, update };
