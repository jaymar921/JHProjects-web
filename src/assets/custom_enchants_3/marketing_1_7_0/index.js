/**
 * The 1.7.0 store page set, copied straight out of
 * marketing/advertising_1.7.0_attachments in the plugin repo.
 *
 * Three images rather than 1.6.0's four. Nothing was added to the catalogue in
 * this release, so there is no sprite sheet and no enchantment table to draw:
 * the hero carries the summary and the two systems that landed get one each.
 */

import hero from "./ce3-1.7.0-hero.png";
import mobs from "./ce3-1.7.0-mobs.png";
import perf from "./ce3-1.7.0-perf.png";

export { hero, mobs, perf };
