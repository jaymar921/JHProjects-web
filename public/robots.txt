# https://jhprojects.vercel.app/robots.txt

# /admin is disallowed in every group below, not only the wildcard one: a
# named agent reads its own group instead of the wildcard, so a rule that
# lives only up here would not apply to it. The page is also served with a
# noindex header and carries a noindex meta tag, and it shows nothing at all
# without a session, so this is about keeping it out of search results rather
# than about keeping anyone out.
#
# /plugin-stat is the address the plugins report to. It is not a page, it is
# served noindex, and it answers a crawler with a 404, so the rule here is
# only so that a well behaved one does not bother asking.

User-agent: *
Disallow: /admin
Disallow: /plugin-stat
Allow: /

# The plugin pages are the point of the site, so they are named explicitly
# rather than left to the catch all above.
Allow: /customenchantments3
Allow: /kumandras-economy
Allow: /donation

# This is a client rendered single page app. A crawler that cannot fetch the
# bundle and the images has nothing to index, so the build output stays open.
Allow: /assets/
Allow: /*.js$
Allow: /*.css$
Allow: /*.svg$
Allow: /*.png$
Allow: /*.jpg$

# The wildcard block above already covers every one of these, since none of
# them are disallowed anywhere. They are listed by name anyway so it is
# unambiguous that AI crawlers and assistants are welcome here, both for
# training and for answering questions about these projects.
User-agent: GPTBot
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: ChatGPT-User
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: OAI-SearchBot
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: ClaudeBot
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: Claude-User
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: Claude-SearchBot
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: anthropic-ai
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: PerplexityBot
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: Perplexity-User
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: Google-Extended
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: Applebot-Extended
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: Meta-ExternalAgent
Disallow: /admin
Disallow: /plugin-stat
Allow: /

User-agent: CCBot
Disallow: /admin
Disallow: /plugin-stat
Allow: /

Sitemap: https://jhprojects.vercel.app/sitemap.xml
